Altahhan CRM - Release 8 + Fix 2

This build restores the operational modules that were missing in the clean branch:
- Leads Excel import (/import_excel)
- Leads Excel export (/export_leads)
- Email templates (/templates)
- Campaigns with Outlook mode (/campaigns)
- Tracking and pipeline pages

Run order:
1) setup.bat
2) run_server.bat

Use one office PC as the server. All other PCs connect from the browser to the shown LAN URL.
Default login: admin / Admin@123
